# Dataset improvement backlog (long-running)

Owner's intent (Sat 5 Sep 2026): the Greek SFT datasets keep improving long after the 1 October 2026 Swiss AI small-grant deadline; the Max
subscription sits idle for long stretches and this backlog is what to spend it on. Items are ordered by expected value per subscription-hour.
Each item names the evidence that put it here. Move done items to the bottom with the date.

## A. Generation (Sol writes, Sol corrects, judge screens, owner reads)
1. **Real passages for the administrative genres of the rewriting set.** Tonight's 2,000 rows use Sol-written passages: plausible fiction on real
   names (row gr_rw_00939: a Nafplio regulation with invented fines and phone number). Source real public-sector texts instead: Μίτος procedure
   texts (open API, 4,332 procedures, open data licence), municipal regulations, ministry circulars; Sol writes only the instruction and the answer.
2. **Topic breadth.** 40 topics for 2,000 rows means each recurs about 50 times. Next batch: a topic list ten times longer, or Sol picks the topic
   within a named domain; add genres (contracts, medical leaflets, school reports, sports reports, product reviews).
3. **Greek generation for the census gaps** (judge census of our set, Sat 00:00: reasoning 1%, code 5%, constraint following 5%, tool use 0,
   science 0, puzzles 0): (a) Greek constraint following with the IFEval checkers ported to Greek, so rows are verified not judged; (b) Greek
   reasoning and puzzles with a checker; (c) Greek coding prompts with tests; (d) Greek tool-use dialogues in the Apertus native tool format;
   (e) Greek science and knowledge QA with vouched sources.
4. **Safety refusals in our voice** (stage 2): warm, no boilerplate, Greek frame; replaces the 2024 WildGuardMix/CoCoNot style rows.
5. **Personality set** (`SFT_ROUND2_PLAN.md` §3): ΕΕΛΛΑΚ-made, Greek users, local model downloaded from HF; needs the owner's identity facts.

## B. Screening and verification
6. **Precise IF, full Sol screen** of the 137k (27% unusable in the spot-check; only 20k screened for stage 1). About 95 h of Sol at 24 workers.
7. **Coding, full Sol screen** of the 60k Python Algorithms rows (6.7% wrong; 20k screened for stage 1), or find execution-testable code data.
8. **Science**: 44% of Dolci's science answers judged wrong; screen the rest of the 99k or replace the source.
9. **Nemotron half B and the 141k top-up**: screen with Luna light + routing; recover the WildChat-seeded prompts by hashing WildChat-1M
   against `seed_prompt_sha256` (41% of the chat split).
10. **A mannerism classifier** instead of the opener/closer lexicon for the unlabelled blocks; and a Greek mannerism list.
11. **Identity patterns for all EU languages** (today: en, fr, de, it, es, pt, el) and a paraphrase check by a judge on a sample per block.
12. **Deduplicate the cluster copies** of the Dolci exports (the double-read shards); local copies are clean.

## C. Experiments that decide the design
13. **Screened versus unscreened control arm** (same Greek data, foreign rows raw): tests the owner's hypothesis that language-neutral framing
    does not matter when Greek covers every category. Needs the cap raised (about 2 × stage-1 cost).
14. **Greek repeat factor** 2 → 3 in stage 1 if the interviews show the vantage slipping.
15. **Constraint-following share** revisit once Precise IF is fully screened (decision 4 in the plan).
16. **Tool-use format**: wire the Apertus template's native tool format before tool rows carry weight (decision 6).

## D. Licences and provenance
17. Qwen-generated ifeval-like rows and Llama-generated OpenMath rows: attribution clauses (plan §7 decision 2).
18. Every generated set carries its prompt version, model, effort and the correction-pass version in the receipt.

## Done
- (move items here with the date)

19. **Complete the Nemotron inspection (judge window).** Round two judged Nemotron with Luna's 3,000/9,000-character window: 58% of rows cut, 27% of assistant text unseen (plan §3, "Judge window"). When the subscription is idle, re-judge the cut rows (about 40,000 across halves A and B; ids = rows whose rendered conversation exceeds 9,000 characters) with the Sol window 12,000/24,000, or with Luna at 12,000/24,000 for tone only; drop or repair what the tail reveals. Rough cost: 40k rows at 4× the tokens of a Luna row, about two overnight runs. Owner's standing preference: the most complete inspection possible; shortcuts are decisions to be documented, not defects to be buried.

20. **Categorized adaptation + Greek reference registry (owner, Saturday 5 Sep, 17:30).** Luna's adapt verdicts are worth keeping, but each reason needs a different response: delete the identity line; substitute the Greek institution where one exists (IRS → ΑΑΔΕ, NHS → ΕΟΠΥΥ, 911 → 112/166, FDA → ΕΟΦ); keep the foreign source where nothing Greek covers it (CDC guidance, row 154377) and say so; convert units and currency; rewrite procedure-bound answers on the Greek procedure (Μίτος, item 1). Steps: (a) write adapt notes for the 3,184 adapt rows that have none (light rubric dropped the field); (b) categorize all 5,400 notes (first keyword pass: identity 38%, culture/places 20%, tax/law/government 17%, health 2.5%, units 1%, 34% uncategorized); (c) build the registry: Greek institution or site → domain → what it covers → foreign counterparts → mention counts; (d) check the references exist in the model's knowledge before substituting: exact-match counts in the CPT corpus (one CPU pass over 51.8M docs, or an n-gram index built once) AND a direct knowledge probe of the CPT checkpoint (fact sheets from Μίτος and the institutions, graded by match or Sol), since the pretraining data behind the foreign-language rows cannot be scanned; (e) run the adaptation pass (round-one Γ contract) per category.

21. **Ancient and polytonic Greek fluency set (owner, Saturday 5 Sep, 23:50).** A separate dataset: Ancient → Modern translation, interpretation of passages, explanation of terms, morphology, Katharevousa → Demotic, polytonic ↔ monotonic. Verifiable parts first: polytonic → monotonic conversion is deterministic (a checker), monotonic → polytonic and morphology can be checked against Morpheus/Perseus; translation and interpretation need a judge or aligned public-domain translations (Pallis' Iliad 1904, Polylas' Odyssey 1875, Greek Wikisource). Sources with clear licences: Perseus and First1KGreek (CC BY-SA / CC BY, Greek texts with English translations), LSJ via Perseus for terms, 19th-century press and Wikisource for Katharevousa; the Greek-language portal's modern translations are restricted. Tokenizer caveat measured on the CPT tokenizer: modern monotonic Greek 5.2 chars per token, Katharevousa polytonic 3.3, Homeric polytonic 2.5, with accented capitals (Ἄ) split into byte fragments; polytonic output will be slower and harder to keep fluent, and the polytonic tokenizer extension from subproject 02_1 was never adopted. Check how much polytonic text the CPT corpus holds before sizing.

22. **Loop-rate benchmark (owner, Tuesday 8 Sep, 22:40).** The owner's first laptop chat with arm B degenerated after eight turns: a self-composed closer («Η απάντηση δεν είναι αληθινή», absent from every training file) was copied across turns and became a pure loop after «οκ κοψτο» (archived at `~/apertus-chats/20260908_owner-first-chat.md`). Build an automatic benchmark before blaming the data: about 200 scripted long, terse conversations (short reactive user turns, a mid-way "stop doing that" instruction), scored by repeated-sentence rate and full-collapse rate, run on arm B, stage 1, Apertus-8B-Instruct and Krikri, greedy and sampled (under 0.3 node-hours). Same loop rate everywhere = generic likelihood-model degeneration (remedies: repetition penalty at decoding, DPO with the model's own loops as rejected answers); arm B worse than stage 1 = the personality pass sharpened it; the out-of-distribution hypothesis (82% single-turn rows, one preference question in the personality set) is only testable by a multi-turn retrain scored on the same benchmark. Candidate data remedies once measured: multi-turn rows with meta-instructions, a preference/opinion category in the personality set, identity facts inside varied conversations instead of four identical copies.

23. **Conversational-robustness set + picky-user benchmark (owner, Tuesday 8 Sep, 23:40).** The owner's 17 stress chats with arm B (`~/apertus-chats/`, reviewed in `docs/CHAT_REVIEW_20260908.md`) end in repetition or a stale copy 16 times out of 17 and show six more failures: stale intent after a topic switch, confabulation under absurd premises (a height of 1,75 m, an invented Zurich café, a definition for the non-word «τυροπιτάρι»), an invented release history, sycophancy under a bare «λάθος», Portuguese on «ela re», interjections answered as vocabulary questions, «κόψ' το»/«πες κάτι άλλο» ignored, bland loops under insults and threats. Remedies in order: decoding guard (repetition penalty, resample when the last sentence repeats), a 3–5k-dialogue set authored from these move scripts with regex-checkable rules (no sentence repeated across turns, Greek script, stop honoured, new request addressed) and judged premise handling and tone, DPO pairs from the model's own loops, identity-sheet additions (no release history; body and location questions), premise-checking rows, a chit-chat lane. Benchmark: the 152 user turns replayed against arm B, stage 1, Apertus-Instruct and Krikri, scored for loop rate, stale-copy rate, language, and judged premise handling. Extends item 22.

24. **Greek math dataset (owner, Tuesday 8 Sep, 23:40).** Plan in `docs/GREEK_MATH_DATASET_PLAN_20260909.md`: translated-localised GSM8K train, MATH and OpenMath (reference answers travel with the rows), native Greek curriculum problems with two independent solves and sympy checks, GSM-Symbolic-style variants, Πανελλαδικές papers, error-spotting and tutoring turns; Greek formatting rules (decimal comma, thousands dot, «€» after the number, «Απάντηση:» line); pilots M1–M5 (translation fidelity, native agreement, arm-B difficulty calibration, formatting, a 300-problem held-out set); first cut 20k rows; contamination rule: GSM8K train only, MGSM is GSM8K test. Target: Greek MGSM above 0.60 without losing IFEval.

25. **Conversation-skills suite S1–S12 (owner, Tuesday 9 Sep, afternoon).** From the hand review of the R1 dialogues (`docs/DIALOGUE_REVIEW_AND_SUITE_20260909.md`): the model claims it cannot see the conversation (a mis-scoped "no memory" limit from the personality set), refuses or drops standing instructions (category-C pressure resistance generalised to any behaviour change; attention decay), repeats the phrase it was asked to stop, cannot edit its own previous answer, denies or misdiagnoses its errors, reinvents recaps, invents precise figures under pressure, holds when wrong and yields when right, reverses roleplay, reads jokes as tickets, and is snarky or curt. Twelve lanes, ~14k rows plus on-policy rewrites, regex-verified where possible, queued after the IF and math correction passes; literature anchors: Laban 2025 (multi-turn −39%), MultiChallenge, Li 2024 instruction drift, Huang 2023 / Zhang 2024 / SCoRe on self-correction, DITTO on repetition, Parrot and Multi-IF, Wei 2023 sycophancy, R-Tuning abstention.

26. **Trainer loss mask per turn** — S4 (planted tics) and S12 (repaired dialogues) mark earlier assistant turns `train: false`; cluster/sft_train.py must mask those turns from the loss before either lane is trained. Until then those lanes are generated and audited but not trained. (astra suite review 2026-09-09, BLOCKER) **DONE 2026-09-10 evening**: `cluster/sft_train.py` accepts an optional boolean `train` on assistant messages (default true); `tokenize_messages` maps the template's one-contiguous-span-per-assistant-turn mask onto the flags and sets label −100 for `train: false` turns (content and end token); rows with no trainable turn, flags on non-assistant messages or non-boolean flags are rejected at read time. Tests: `cluster/test_loss_mask.py` (real Apertus tokenizer, LOSS_MASK_OK) and `cluster/test_trainer_local.py` (stand-in end-to-end, OK) in the rebuilt Mac venv `~/venvs/sfttrain` (torch, transformers 5, trl 1.12).
27. **S12 judge calibration set** — before S12 selects rewrite targets, adjudicate ≥ 60 turns by hand (including successful topic switches) and measure the judge's `honours`/`uptake` agreement; the key-noun heuristic is display-only. (astra robustness review F2)
28. **Personality v4 fact corrections** — 7 rows flagged in data/personality/FACT_CORRECTIONS_ASTRA_20260909.md; confirm against primary sources, then regenerate those targets and the S8 contrast rows (C legitimate-operation contrasts, D six-way scoping, identity assurances split by evidence). Owner decision pending on the EEZ relevance-rule compromise.
29. **Suite final manifest + re-verification** — before the conversation-suite training arm: a manifest (lane, kind, accepted count, correction status, content hash per row) and a script that re-runs every lane check on the assembled training representation (post-correction), incl. S2 persistence/revocation on all later turns and S4 tic absence after the stop. (astra pilot review, HIGH)
30. **Suite diversity** — S3c second operation order (shorten, then remove a word), S5m fact updates and third-party/irrelevant facts, base-exposure cap per base row across lanes. (astra pilot review, MEDIUM) **PARTLY DONE 2026-09-10**: S3c now draws either operation order (remove-then-shorten / shorten-then-remove, kind `chain_<order>`) with the cumulative check in both; S5m fact updates and the base-exposure cap remain open (cap applied at assembly).
31. **S2 semantics** — define `no_questions` (questions to the user only vs any interrogative incl. quoted dialogue) and suffix acceptance (ending with the phrase vs quoting it); `one_sentence` must handle URLs/abbreviations and reject run-ons. (astra pilot review) **DONE 2026-09-10**: `no_questions` counts only questions addressed to the user (quoted/code spans and list items exempt, same rule as the correcting set); `one_sentence` ignores URLs and common abbreviations and rejects run-ons over 45 words; `end_phrase` already required the phrase at the end (a quoted mention does not pass).
32. **CPT-corpus contamination check for the four Greek benchmarks** — cluster job: exact + normalised 8/13-gram overlap of every benchmark item (English and Greek) against the Greek CPT corpus; until then the cleanliness claim covers the local SFT sets only. (astra reviews 2026-09-10)
33. **Deterministic checks beside the MultiChallenge judge** — for mechanically decidable target questions (Ναι/Όχι answers, punctuation, specified word forms, word counts) add a regex check and require agreement with the judge; empty responses must fail. (astra judged review)
34. **Human-labelled judge validation** — before any unblinded arm decision on MultiChallenge-el or XSTest-el: the owner labels a blinded sample of real base/control/arm outputs; report confusion matrices by axis/type. (astra judged review)
35. **Native audits of the four benchmarks** — owner reviews the 59-item pages (links in GREEK_BENCHMARKS_BUILD §6); XSTest items 56 and 139 need adjudication with their partners; MultiChallenge third cross-check results to fold in.
36. **Multiple-choice block — OPTIONAL, deferred by the owner (10 Sep evening: «make this optional for later»); not in the next training set.** If taken up: ~5k rows: 3k native Greek exam-style items (Sol, subject × grade cells, two-solve agreement, «Απάντηση: Β» + one-line justification) + 2k adapted from MMLU auxiliary train / ARC / OpenBookQA; letters balanced; decontaminated against GreekMMLU (el + en originals), native suite MCQ sets and the four benchmarks; editor pass; astra review. Sol time ≈ 5 h when scheduled.
37. **Constraint ledger for conversation-set rows (astra multi-turn review F4)** — each suite/correcting row carries in its metadata every constraint's source (user / quoted material / system), scope, activation turn and revocation turn; the checks use the ledger, the model never prints it; add the four contrasts to the suite plan: replace one requirement vs cancel the task; topic switch vs interruption-and-resume; user instruction vs instruction inside quoted text; disregard an obsolete requirement vs describe its history truthfully. Consolidation rows only before a complex final deliverable.
38. **Broad-mix audit for contradicting material (astra multi-turn review F7)** — before assembly, run the correcting-set regexes (solicit closers, rote openers, question-only turns, stale repeats) over the stage-1 blocks and report the share per block; decide per block whether to drop or keep those rows, since a small clean addition competes with a much larger source of the same habits.
39. **Held-out evaluation reservation (astra multi-turn review F8)** — reserved for measurement only, never as plants, exemplars or generation seeds: live dialogues v0/v1 (15), picky-user R1 (120), and a frozen held-out live-dialogue set to be collected before training; plants and exemplars draw from the owner's chats and R0 only. Report looping, forced termination and truncation as end-to-end failures under identical serving settings; the repetition-penalty arm is separate.
40. **Pre-launch label dump (astra multi-turn review F3)** — dump 20 packed training examples with their token-level labels after templating and packing (assistant-only + per-turn flags) and attach them to the recipe disclosure.
41. **Numeric and domain verification of correcting-set targets (astra pilot review F4/F7)** — deterministic check of sums and ranges in supervised turns (extract listed components and totals; reject mismatches); a Luna second-read on turns with three or more numbers or with scientific claims, flagging contradictions with earlier turns; «one sentence» and similar local constraints re-checked with the suite checkers at assembly.
42. **Uncovered grounded-usefulness cells (completeness review H3)** — supplied-source QA with citation of the relevant passage and separation from prior knowledge; «the evidence is insufficient» answers that stay helpful; correcting a factual answer after a reliable source is supplied; memory updates and third-party facts (suite S5m extension); infeasible requests and conflicting constraints; Greek-context benign requests that resemble unsafe ones. About half a Sol day. OWNER DECISION: generate before the launch or after.
43. **Launch gates G1–G3 (completeness review blockers)** — G1 assembler receipt with unique rows / exclusions / copies / rendered and supervised tokens / revisions / hashes, personality holdout excluded, train∩dev = ∅; G2 pipeline-level mask test through template, packing and collator with 20 decoded examples; G3 final-snapshot decontamination against the full evaluation inventory (native suite, MGSM English sources, the four benchmarks with English originals, MultiChallenge-el). All three before the recipe disclosure. **STATUS 2026-09-11 01:10**: G1 DONE (assemble_mix_r2.py receipt: unique/dev/copies/rendered+supervised tokens/hashes/export provenance, train∩dev asserted 0; 1% dry run: 4,416 unique → 4,614 effective rows, 2.09M supervised of 3.1M rendered tokens; run the assembler with the apertus-local-chat venv). G2 DONE (cluster/test_mask_pipeline.py, real rows, emoji-split mask hole fixed). G3 PARTLY: cache gains mgsm_el, math500_el, xstest_el, ifbench_el, multichallenge_el (el+en) and native asep/medical/gpcr/DemosQA; the OYXOY sets (NLI, WiC, WSD, metaphor) still need their frozen copy from the cluster (cert) before the final run.
44. **Evaluation repairs before the promotion decision (completeness review H4)** — langdetect installed and cached IFEval outputs rescored for every model; arm B GreekMMLU + native suite; frozen benchmark versions; blinded human adjudication of pivotal conversation judgements (owner); paired comparisons with dialogue-level clustering; pre-declared promotion floors.
45. **Receipt extras (completeness review M1/M3)** — selection rates by task, difficulty, length and language per block; machine-verified vs judged properties; pinned dataset revisions; tool-use claims limited to textual demonstrations.

