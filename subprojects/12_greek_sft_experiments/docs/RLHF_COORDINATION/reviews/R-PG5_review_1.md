VERDICT: HOLD

1. **Blocking — the exported active set contains five superseded prompt versions.** In `data/rlhf/round3/round3_all.jsonl`, records R3-0243 and R3-0378 contain older logical IDs and messages than their active replacements at `generator_v02/runs/R3-fix2/prompts.jsonl:1,5`; R3-0253, R3-0334 and R3-0377 remain exported despite having no active replacement (`R3-fix2/prompts.jsonl:2-4`). Thirteen regenerated active IDs also resolve through `registry.sqlite` aliases to earlier superseded logical IDs because `generate.py:272` uses `INSERT OR IGNORE`. In addition, forum records R3F-0001–R3F-0039 are absent from both `prompts` and `aliases`; `stage.py:19-21` creates their active identities only in the export. This breaks criterion 1 and makes downstream sampling and owner review use the wrong or unregistered prompt. Fix: update aliases on supersession, register forum selections with their source provenance, finish the terminal dispositions, rebuild `round3_all.jsonl`, and assert that every exported logical ID is the single current keepable registry row.

2. **Blocking — the realised distribution and held-rate report do not exist.** The manifest targets are recorded at `manifest_round3.json:25-132`, but no before/after distribution or per-cell terminal-held report was produced. Independent reconstruction finds 408 active generated prompts plus 39 forum prompts, versus 483 single-turn manifest slots, with 36 terminal holds. Examples are instruction/el 79/89, safety/el 32/39 and everyday/en 58/64. Realised seeded axes also differ substantially: routine 49/133, Greeklish 7/22 and detailed 71/22. The run receipts report only per-run totals, which are non-additive after retries and supersession. This violates blocking criterion 2 and the reporting half of major criterion 6. Fix: publish a terminal, deduplicated report by joint cell and by each seeded axis, including manifest target, active count, held count/rate, deviation, and reason.

3. **Blocking — changed labels are discarded by the export, and the latest active rows are unverified.** `stage.py:15-17` exports only the realised labels and drops `labels_assigned`. There are 244 current active source rows with both assigned and verified values, but zero assembled rows retain `labels_assigned`. For example, R3-0001 was assigned routine/short and verified as compositional/detailed (`R3-main/prompts.jsonl:1`, `verification.jsonl:1`), while `round3_all.jsonl:42` contains only the latter. The new active R3-fix2 records R3-0243 and R3-0378 have no verification records at all. Furthermore, `generate.py:118` and `verify.py:27` search for the obsolete prefix “Detail is defined by content”, while glossary v1.0.3 uses different wording at `seed_label_definitions_v1.md:61`; the corresponding assertion at `test_generator.py:54` fails. Fix: repair the glossary lookup, rerun independent verification for the final active versions, retain both assigned and observed labels in the assembled export and page, and make the test suite pass.

4. **Blocking — active prompts still contain prohibited framing or verified task drift.** R3-0004 remains active and says “Using product information available by June 2025” (`round3_all.jsonl:371`; `R3-validation-50/prompts.jsonl:1`), directly contradicting the no-time-scope rule at `generate.py:84`. New active record R3-0378 again embeds “au 30 juin 2025” (`R3-fix2/prompts.jsonl:5`). R3-0098 remains active even though `verification.jsonl:374` reports `fidelity="missing_given"` because the message never says that the reservation is for plants; `verify.py:78` excludes `missing_given` from regeneration. These defects violate criterion 4 and undermine fair answerability. Fix: treat missing required givens and prohibited time-scope framing as regeneration conditions, regenerate these three records, verify the replacements, and rebuild the export.

5. **Blocking — specialist-maths execution is incomplete.** All 247 currently marked `maths` or `embedded` records appear in `maths_prompts.jsonl`, and the intended split at `stage.py:48-57` is correct. However, `maths_references.jsonl` contains only 198 IDs, leaving 49 without references, including R3F-0001–R3F-0039. There are no `judged_maths_s*.jsonl` outputs. Thus the requirement that every purpose-maths and embedded-maths item actually reach specialist judging is not satisfied. Fix: rebuild routing from the corrected active set, complete references for every routed prompt, run the specialist judge, and assert exact routed/reference/judged ID coverage.

6. **Major — the required owner review page and judged pair data are absent.** `page.py:9-10` requires `round3_pairs.jsonl` and `round3_summary.json`, but neither exists. There is also no `round3_samples.jsonl`, general judgement output, maths judgement output, pair output, summary, generated `index.html`, or page data chunks. Consequently, the owner cannot inspect any judged reply, reason, chosen/rejected pair, or maths reference, failing criterion 7 and the stage goal. Fix: complete sampling and both judging routes, build pairs and summary, generate the page, and verify that every prompt exposes all required fields.

7. **Major — two principal runs lack the claimed executed-source snapshot.** `R3-validation-50/receipt.json:17` and `R3-main/receipt.json:17` bind hashes `dab615c…` and `aa64f817…`, but neither directory contains `generate.snapshot.py`. These runs supply 370 of the 408 current active generated prompts, and the current source hashes differently. This contradicts the brief’s provenance claim and prevents verification of the exact executed code. Fix: recover and archive bytes matching both receipt hashes, or explicitly mark the provenance unavailable and regenerate under a retained snapshot.

Verified as correct:

- Every one of the 408 current generated active rows has a manifest slot, unique reserved instance, completed reservation, clean final code check, passing run review, and receipt-matching code hash.
- All superseded registry prompt rows have `keepable=0`.
- No duplicate active instance key or exact scenario was found; the generator’s cross-language similarity check found no active pair, and no exact or threshold-level near-duplicate message was found.
- The 36 terminal held slots have substantive final defects supported by their code checks or reviews.
- All 39 selected forum sources are distinct and recorded as `gate_accepted`.
- The static routing rule correctly sends maths-only prompts to the specialist route and embedded maths to both specialist and general routes.