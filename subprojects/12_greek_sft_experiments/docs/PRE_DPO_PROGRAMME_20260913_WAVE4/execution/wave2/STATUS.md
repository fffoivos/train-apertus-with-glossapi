# Current result update — D016

The fourteen-source training probe completed42 model requests with no model retry. Both blind solutions and the original references received full root review. All fourteen tasks preserve their maths and have valid complete solutions. The default high solution is selected for thirteen; the blind verifier is selected for the polynomial case to avoid a needless assertion that real roots were an explicit source hypothesis. A trigonometric-domain flag resolves under the source's ordinary natural-real-domain convention and does not trigger artificial source repair. Greek correction returned13 unchanged texts and one optional wording proposal, declined after review. No new semantic source repair or xhigh escalation was required. Together with seven fully reviewed older Level5pairs, the21-row package passes actual reader/tokenizer/decoded-mask checks:8,308 inputtokens,5,635 supervisedtokens,maximum648. It remains a staged candidate package, not an expanded production mix.

One root launch mistakenly ran inside the filesystem sandbox. All22 local attempts failed app-server initialization before any model request; empty events and matching startup errors were checked and preserved. The corrected execution used a separate explicitly bound runtime state after a one-call probe. These22 launcher failures remain visible separately from56 actual stage model calls (42 generation+14 Greek) under the72-model-call ceiling. The immutable original runner and failed receipts were not overwritten.

Krikri job3390957 saved all2,926 fixed-suite generations and all791 ILSP generations, then failed while formatting its terminal table because chardet.detect was unavailable. Slurm FAILED is retained. Validate the saved per-item and aggregate evidence, and recover fixed scoring on CPU without repeating inference. Report generation, scoring and scheduler status separately. The reported22m17s allocation is approximately0.3714 node-hours/CHF1.00; final ledger integration waits for the accounting receipt.

Development-dialogue source review again found that an asserted new date can be mistaken for unsupported inference, and that apparently disjoint inventory counts can overlap unless stated. Fix those source semantics before authoring and record each change. Coffee updates need an explicit draft-order contract so a text revision never claims an external order was placed. This is additional evidence that executable labels alone cannot verify natural-language scope. Keep the final families sealed. Prepare20 genuinely varied new TRAIN scenario families (60decisions) before scaling toward600; numeric/name substitution alone is insufficient diversity.

Prepare independent, maximum-one-attempt CPU source pilots for lossless Dolci200 and a212.93MB shard of OpenMath's documented train_1M subset. Each requested30-minute node has a separate planningmaximumCHF1.35; only exact reviewed readiness can commit it. The OpenMath pilot uses strict source-problem matching, explicit family exclusions and at mosttwo solutions per selectedfamily, not the full7.576GB train split. Self-script and contract hashes are checked before writes/downloads. Candidates must still pass proof review, token/mask and full assembly gates.


---

# Current result update — D015

The 32 training dialogue decisions passed full semantic review after four root scope repairs, independently checked: current versus historical booking, excerpt versus whole-world route claims, and ambiguous record cancellation versus global disengagement. Greek review returned 26 unchanged examples and six proposals. Four local phrasing edits were accepted; two optional changes to valid Greek were declined. The final projection passes the real reader, tokenizer and decoded supervision check: 4,981 input tokens, 993 supervised tokens, maximum 223. Only the intended final answers and their end markers are supervised. All prior authoring and repair versions remain preserved. The pilot has used 68 calls under the 118-call cap; development and final generation are still separate and pending. This is 32 accepted training decisions, not the 600-row target.

The complete MATH source inventory identifies 7,492 normalized families in 7,500 rows. Current Greek training includes 3,240 families. None of the pilot's 16 fresh Level-5 families maps to the current Greek MATH train/dev records, but that is not a claim of absence from pretraining or other uninspected sources. Frozen next-stage options exclude all known current train/dev families, earlier pilot families, ambiguous lineage and MATH-500 overlap. The overlap percentage uses each candidate problem's distinct 8-grams as the denominator; actual assembly's pooled-index check remains a separate gate.

Next maths stage: fully adjudicate seven existing clean Greek Level-5 pairs, then a 14-source training probe with high adaptation, high solving and blind high verification (42 primary calls). Permit at most 14 Greek checks, eight defect-driven source/solution repairs and eight retries: absolute cap 72. Do not run xhigh on a fixed half of examples; escalate only concrete unresolved cases. The earlier source-inventory report's illustrative xhigh schedule is superseded by this decision.

A bundled Python path in the maths package's reproduction notes was wrong. The actual local training environment reproduced all previously reported maths token counts exactly; its additive correction receipt preserves the original metadata. A separate extra parent traversal in the dialogue metadata reference is corrected additively. Neither correction changes training text or original measured results.

GreekMMLU's snapshot contains dangling parquet links, so existing directory presence does not establish usable data. Acquire revision-pinned source bytes before the 200-item content gate. The current Krikri allocation continues; no extra GPU allocation follows from this data repair. OpenMath source metadata is pinned, but only its README is cached: prepare a bounded CSCS CPU intake of original MATH families, keeping alternate downsampled splits separate and limiting repeated solutions. Latest shared weekly Codex quota shows 44% remaining, not a per-task usage figure.


---

# Current second-wave execution status

13 September 2026. Goal active; no new model training or dataset release.

- Maths: 32 reviewed candidates, including 24 new/rebuilt and eight retained controls. Exact tokenizer and control-character gates pass. Sixteen fresh Greek checks retained the existing text; 108 total pilot calls.
- Instruction following: 20 independently reviewed candidates, all passing declared constraint checks; 14 actual text changes and six retained controls.
- Trained maths: seven bounded repairs accepted independently, with before-message bindings.
- Personality: 42 candidates root-reviewed; four final deltas independently accepted. Six new closures were never in historical training.
- Dialogue: revised four-decision probe passed semantic review; 28 remaining training decisions are being authored. Dev/final remain separate; final content is sealed.
- CSCS: Krikri v1.5 job3390957 is running after its 64-generation smoke passed, at a maximum85minutes/CHF3.81 inside the four-node-hour measurement allowance.
- Next: read full dialogue outputs; complete source-family inventory and lossless tool pilot; freeze the maths screening data and training contract. Main training and final competitive claims remain pending.
