# Evidence-driven plan decisions

Append new decisions after every meaningful result. Change the current plan as well; do not leave an obsolete instruction active merely because the correction is buried here.

## D001 — selective repair and rebuilding, 13 September 2026

Evidence: first-wave 300-row unfinished maths audit and 19-case targeted adjudication; dialogue/IF/import audits; 12-dialogue authoring/correction/semantic-repair pilot. See `execution/STATUS.md`.

Decision: no wholesale Greek restart. Preserve sound adapted examples; repair local semantic or language defects; re-adapt sources that failed the cultural/teaching contract; rebuild defective scenario designs; reconstruct uncertain maths pairs from originals; re-export clipped tools. The original adapted-core paired audit and the already-trained maths audit are still required before broader source-level decisions.

Consequences: the tool export, scorer empty-response gate and maths serialization/provenance guards were repaired. Greek lexical blacklist proposals were withdrawn. The small dialogue pilot was semantically rebuilt where its unresolved claims were incorrectly specified. These changes do not establish that dataset errors caused historical benchmark drops.

## D002 — strengthened goal and second wave, 13 September 2026

Owner expands success to the strongest fair Krikri comparison, all-round benchmark/dialogue performance, Greek quality and transparent release materials. `GOAL.md` defines the active goal. Keep the targeted foreign-language retention objective and existing budget.

Dispatch: three Sol/high agent tasks for adapted-core/foreign pairs, IF repair pilot, and fair-peer/release evidence; one bounded 120-row trained-maths audit plus separate known-failure controls, comparing original versus corrected targets and actual assembly membership. No GPU allocation is made for this wave.

Next decision: per source/family keep, repair, readapt or rebuild, based on verified semantic/editor damage and provenance. Competitor selection and endpoint definitions are frozen before candidate comparisons, not chosen from their outcomes.

## D003 — both Krikri releases are critical peers

Evidence: `execution/wave2/fairness_and_release/`. Original and v1.5 trade instruction-following and dialogue results; v1.5 has no matched local results. Pin both separately, complete the missing critical cells, and preserve native templates and equivalent tasks. A stronger-per-endpoint summary may supplement the two full columns; no per-item hybrid peer. Current source revisions are not retroactive evidence of historical source identity. Licensing remains source-specific.

## D004 — retain the adapted core, repair IF task design

Evidence: `execution/wave2/adapted_core/` and `execution/wave2/instruction_following/`. The 24 inspected Greek core pairs support retention (23 keep, one local repair). The 18 paired foreign rows cover EN/FR/DE only; ES/PT/IT nine-row checks are unpaired and separate. Review and adapt those languages explicitly; do not pretend existing retention imports were already adapted. Two current raw comparison assembly rows have unresolved lineage drift.

All 20 selected IF originals passed the deterministic checker, despite ten established task/specification failures and one protocol ambiguity. Root revision chooses lexical identity explicitly for the word-frequency task, preserves a separate exact-surface contract elsewhere, and improves one awkward collocation. All 20 revised candidates pass the checker; four require factual-source review and none is promoted to production. Retain both immutable pilot versions. Scaling requires semantic task review as well as counting constraints.

## D005 — trained maths review completed; causal verdict remains open

Evidence: `execution/wave2/trained_maths/raw_summary.json`. All 122 records returned in 63 calls: 120 stratified actual-training rows plus two separate targeted controls. Raw results are 109 no-defect, 11 issue, two uncertain. The correction effect was 80 unchanged, 41 language improvement, one semantic terminology improvement, with no reported introduced damage. These raw judgments require adjudication and do not prove zero corpus damage or explain a benchmark decline. One response was manually accepted after the audit validator incorrectly rejected findings about an already-repaired BEFORE defect; its failed receipts remain intact and no new model call was used.

## D006 — personality: factual deployment claims and missing closing targets

Evidence: `execution/wave2/personality/`. Full 192-row v4 identity/deployment audit routes 157 retain, 29 deployment-conditional rewrite, six missing-final-target repair. Root checked actual train/dev membership: all six malformed endings are absent, and every retained personality record ends assistant. Do not attribute historical model failures to training those six malformed rows. The trained block contains 1,504 unique rows at four copies (6,016 presentations). Review repetition and finishing exposure separately.

Prepare a compact accurate identity/manners core, rebuild the missing closures and condition capabilities on available runtime evidence. Weight/code/data release, Apache licence and cutoff statements need checkpoint/data provenance; current proposals do not establish facts. Seven registered ordinary-world fact corrections still require primary-source checks. The twenty repair candidates are drafts, not promoted corpus changes. Preserve historical generator identity separately from new repair authorship.

## D007 — factual checks change IF targets as well as formatting

Root version 3 of the twenty-row IF pilot attaches four primary-source checks. It dates donor registration, removes an assumed hardware management interface, distinguishes dental care from immediate hospital escalation for swallowing/breathing difficulty, and replaces a generic legal citation with the actual provisions. All twenty retain deterministic checker passes. This is additional evidence that passing a formatting checker or a single author review is not sufficient. Independent semantic review remains required before promotion.

## D008 — native GreekMMLU score was a custom protocol

Pinned upstream code proves that official GreekMMLU scores Greek option labels under a subject-specific prompt, whereas our frozen evaluator scores full answer texts by average token log probability under a generic prompt. Krikri reaggregation is 52.0082% micro/53.6175% subject macro, not 66.47%. Preserve old results as the custom full-answer diagnostic; prepare a 200-item matched old-versus-official protocol check for original Krikri and G3F2P1 before interpreting knowledge competitiveness or selecting MC training. See `execution/wave2/greekmmlu_protocol/report.md`. No new GPU score or causal conclusion yet.

## D009 — adjudicated maths defects become bounded repairs

Independent review of 26 distinct full rows rejected the proposed automatic edits to κατάστημα της, της Έμμα and the concise correct digit proof. Four prompt-level repairs, one proof repair and the two separate targeted unit/terminology controls are now seven concrete candidates in `execution/wave2/trained_maths/repairs_v1/`. Arithmetic checks pass; independent repair review remains. No blanket re-adaptation or editor-damage claim is justified. A missed area-versus-volume label in the first review also shows why raw Sol passes are not proof of perfection.

Budget reconciliation: latest ledger CHF 163.02 used / CHF 66.98 remaining under CHF 230. The full 42-node-hour planning programme still exceeds current headroom by CHF 46.00; first measurement/screening envelopes fit, and the main run needs concrete repricing or an explicit later cap decision.

## D010 — accept bounded repairs and preserve full dialogue training target

IF v4 has 20 independently accepted candidates and 20 checker passes. Metadata now names one current record. Official-source review supports the donor application wording; a reviewer flag alone must not cause a false repair. All seven trained-maths repairs passed full independent checks. These are accepted pilot candidates, not a claim about whole-corpus quality or a production promotion.

Dialogue specification revision 2 preserves 600 training decisions and adds 60 development plus 60 final-confirmation decisions (720 total). Its 48-decision pilot keeps 32/8/8 and now includes cancellation in training. Final families remain separate. Prepare bounded authoring with executable checks, then inspect generated failures before scaling.

## D011 — strict output schema fixed; maths generation pilot proceeds

The four-call probe initially failed before generation because nested open JSON objects were incompatible with the API strict schema. All failed receipts and the previous dispatch revision remain preserved. The corrected schema and local checker passed; rerunning the same four public MATH inputs produced four faithful Greek adaptations in 16.18–21.66 seconds each. Root reviewed all four texts and accepted the probe. The frozen 88-job master queue is now running with eight workers; four schema failures count toward the unchanged 136-call ceiling. Solving and solution-blind verification never receive stored reference answers.

A staged accepted patchset records 27 reviewed rows:21 actual text overlays (7 maths,14 IF) and6 unchanged controls. Applying them to a future assembly requires exact before-message hashes, existing family splits and masking, and tokenizer/token-weight checks; no historical corpus is overwritten.

## D012 — completed maths pilot and dialogue probe change the next steps

Maths evidence: `execution/wave2/maths_generation_pilot/`. All 88 frozen jobs returned in 92 attempts, including the four pre-generation schema rejections. Full semantic review routes eight reused defective solutions to regeneration and retains eight sound old controls. Sixteen fresh Level 5 problems passed solution review, with two declared source repairs: explicit independent uniform arrivals, and a possible-value question where the original falsely implied uniqueness. Final candidate assembly will combine 24 new/rebuilt examples and eight retained controls; no historical ID match is inferred for partial-cut records. One unselected high-effort geometry solution contains two form-feed characters, proving that task correctness and serialization need separate checks. The eight high/xhigh pairs are descriptive evidence only, not proof that increasing reasoning effort solves quality problems.

Observed usage: 1,098,460 input tokens (571,392 cached), 75,074 output tokens (28,089 reasoning). These subsets must not be added twice. The successful run spanned about 316 seconds including the probe and coordination; review and assembly take additional time.

Dialogue evidence: four train-only authoring calls returned valid structured responses, but the semantic probe failed. The author added a hidden 60-minute claim to an unresolved request, turning it into a clear user update, while the target still asked for confirmation. Other cases relied on unsupported assistant assertions or claimed an external reminder cancellation without a tool. Revise the contracts before expansion: model-visible evidence determines the label; decision wording is a semantic ceiling; clear new user information updates state; text-record edits and external actions have separate contracts. Preserve all four original calls and count repairs under the existing 118-call ceiling. Final-confirmation content must remain excluded from prompt selection.

Personality root review examined 42 candidates and proposes four local deltas: relevant farewell, focused privacy answer, conditional handling of an offered image, and landlord rather than relative. Source checking alone did not prevent the last speaker-relationship error. Independent delta review remains pending.

## D013 — first peer allocation; maths and personality candidates accepted

Root verified the final `edf6f76f` peer bundle, all six receipt hashes, science manifest, model/tokenizer control hashes and exact scheduler test. Job3390957 started at13:44:05Z on one debug node, maximum85minutes/1.4167node-hours/CHF3.81. It is the first commitment inside the existing four-node-hour measurement allowance. No retry or partition change is automatic. Final usage waits for accounting; the baseline ledger remains CHF163.02 used, CHF66.98 unspent, with CHF63.17 uncommitted after this maximum reservation.

The 32-row maths package passed complete decoded-control scans and actual pinned CPT tokenizer checks:13,681 rendered tokens,8,741 supervised tokens, maximum1,003. The all-output scan found a third form-feed character in the selected blind-verifier candidate for problem3775; the already-reviewed clean high solution replaced it. The other two characters were in the rejected high geometry solution. Preserve all originals and conflict history. No blanket xhigh escalation follows: the eight paired proofs mostly agree, and both efforts missed the nonunique source question. Keep high as default and escalate on concrete unresolved cases.

A bounded16-call Greek language check of the fresh accepted examples is running. It preserves mathematical spans, assumptions and source repairs, accepts unchanged good Greek, and remains inside the original136-call pilot ceiling (92already used plus at most20additional calls). This broadens the earlier defect-trigger-only polish into a measured quality check before scaling. Root accepted the revised dialogue cores after five remaining history-versus-new-state ambiguities were removed. The four-probe launch follows the shared worker queue.

Independent review accepted all four root personality deltas. The42records now have root full-context review, with independent review on the four final changes; the previous candidate author is not counted as an independent reviewer of its own work. Historical sources remain unchanged.

## D014 — Greek check preserves valid text; dialogue expansion proceeds

All 16 fresh maths Greek checks returned with no retries: 12 unchanged and four optional wording proposals. Root read all four deltas in full context and retained the original valid article/preposition/word-order variants. No mathematical changes or invalid controls appeared. The accepted 32-row package remains byte-identical; the maths pilot has now used 108 calls out of its 136-call ceiling. Keep the correction prompt charitable and adjudicate proposed edits instead of forcing uniform phrasing.

The revised four-dialogue probe passed full root semantic review and recomputed state, role, mask and visible-evidence checks. Its next stage is the remaining 28 frozen training decisions at eight workers, with at most four retries in this dispatch. Development and final-confirmation generation remain separate. Greek review may polish mechanical wrappers while preserving the truth and state contracts. The four original failed-semantic candidates and four repaired versions both remain available.

The Krikri measurement passed its 64-generation smoke with zero request errors and is now generating the full fixed suite and ILSP tasks. Exact matched IDs and context exclusions remain mandatory for comparisons. No benchmark score or winner is claimed from a successful serving check.
