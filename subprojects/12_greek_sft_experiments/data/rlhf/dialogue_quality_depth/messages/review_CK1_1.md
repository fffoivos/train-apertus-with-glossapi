VERDICT: HOLD

1. [blocking] `rollout.py:159-167,194-241`; `test_dqd.py:147-159` — restart is not idempotent after an assistant snapshot or completed Sol follow-up call. An active trajectory ending in an assistant message is skipped forever on restart, leaving it unterminated. The test only replays `sample_assistant`, not the rollout state machine. Required fix: reconstruct pending follow-ups from assistant-ended active snapshots, reuse deterministic cached Sol results, and add crash-boundary restart tests after target completion, assistant snapshot, and Sol completion.

2. [blocking] `forecast.py:18-30,46-61,84-95` — admission does not prove that all admitted turns can be annotated within the remaining reservation. It projects measurement depth from the three-turn smoke mean, and computes continuation calls as one global `ceil(total/8)` although batching is separated by depth wave. It also derives annotation throughput from every smoke-phase Sol call, not annotation calls alone. Required fix: budget the full admitted horizon conservatively (or another explicitly justified upper bound), sum continuation batches per wave, measure annotation calls specifically, and admit only a size whose raw generation, continuation, and complete annotation fit the phase and total reservations.

3. [blocking] `manifest.py:84-92,202-219,232-241` — joint allocation is not fully repaired or exported as required. Compatibility is checked only at task level before an unconstrained fixture family is chosen; the frozen manifest consequently assigns revision interactions to incompatible families such as `e_troubleshoot` and `s_general` (`ME006`, `ME013`, `ME022`). Moreover, `actual_joint_counts` contains only independent axis marginals, not actual joint cells or before/after repair evidence. Required fix: enforce interaction compatibility at fixture-family selection, regenerate the manifest before model calls, and export composite joint counts plus an explicit repair receipt.

4. [blocking] `manifest.py:46-57,148-188` — generator-family isolation is only a one-time initialization snapshot. Existing manifests return without rechecking the live generator registry, so a subsequently reserved generator family could reach measurement. `immutable=1` can also conceal committed WAL state if the generator switches journal mode. Required fix: use a normal `mode=ro` connection and revalidate all admitted families and instances immediately before measurement openings/rollout, aborting on any collision; record the checked registry snapshot in a receipt.

5. [blocking] `select.py:101-149` — selection does not implement §9 under capacity pressure. Controls are restricted to one per trajectory even though the allowed two-prefix cap could satisfy the required control quota; a reproduced 24-prefix case with six eligible controls across three trajectories selected only three controls. P/R shortfalls caused by trajectory caps are not recomputed from remaining eligible cells, the generic redistribution messages do not record each redistribution, and the P/R shuffle is nullified by sorting on unique trajectory IDs. Initial controls also lack the global duplicate-prefix check. Required fix: use a two-pass control allocation, enforce a global prefix set, recompute remaining P/R quotas after every capacity shortfall, retain a seeded tie-breaker while spreading task/language, and log each old/new quota.

6. [blocking] `analyse.py:173-178`; `annotate.py:219-249` — the §7 calibration gate is bypassable. `report` freezes successfully when calibration/review files are absent or when only a subset of the required sample and boundary turns was adjudicated; it checks only an optional `rubric_revision_required` flag. Required fix: persist the required calibration-ID receipt and refuse report freezing unless all required 24-or-fewer sampled rows and every mandatory boundary row have review decisions, with the disagreement calculation complete.

7. [blocking] `test_dqd.py:147-159,226-239,248-267` — the green 22-test suite does not exercise the acceptance properties above. In particular, the required crash-mid-wave test stops at the call-cache helper, the “joint counts” test checks one marginal only, and selection tests do not cover reusable control capacity or cap-driven redistribution. Required fix: add end-to-end offline regression tests for these failure modes and keep the suite free of sockets, Codex processes, and home-directory reads.

8. [major] `openings.py:22-31`; `manifest.py:28-35,220-221` — smoke scenario assignments are stored but never shown to the opening writer. Therefore the six smoke chats are not guaranteed to exercise ordinary continuation, requirement change, retention, conditional correction, genuine completion, and uncertainty as required. Required fix: pass a public, non-evaluator scenario instruction into opening construction, preserving the rule that correction occurs only if the observed response warrants it.

9. [major] `forecast.py:106-130`; `manifest.py:148-161` — `forecast.json` is overwriteable after measurement openings or outcomes exist, contrary to choosing and freezing admission before observing measurement. Required fix: refuse forecasting once any measurement artifact is nonempty, freeze the forecast hash in `receipt.json`, and verify that receipt whenever admitted seeds are loaded.

10. [major] `dqd.py:95-99` — argument-parsing failures occur before the exception handler, so commands such as a missing subcommand terminate with argparse output and no required `DQD_FAIL` line. Required fix: route parser errors through the machine-readable failure path and test malformed/missing arguments.

Verified correct:

- Opening prompts exclude references, checks, and fixture parameters; user-simulator prompts contain only public user state and visible prefixes; annotation batching excludes suffixes and duplicate trajectories; ranking prompts hide candidate provenance.
- Raw and candidate sampling use `n=1`, temperature `0.8`, `top_p=0.95`, `max_tokens=1500`, and no added system message. Stored completions retain whitespace and SHA-256 hashes. The six terminal codes are distinct, including `length` → `truncated_output`.
- Call reservations are persisted before target/Sol requests; failed and ambiguous attempts remain counted; total and phase Sol limits are enforced cumulatively.
- The current runtime manifest records horizons 3/8, contains 6 smoke and 35 measurement rows, and has 41 unique locally reserved families. Its receipt hash matches, and no current family or instance overlaps the generator registry.
- Export requires a reinforcing chosen answer and a substantive ranking, records ties/all-bad outcomes in `rejected_pairs.jsonl`, checks shared-prefix hashes, and documents completion-only masking including earlier assistant turns.
- The EUR 5/4/1 constants, GPU start/stop ledger, and conservative deadline helper are present.
- `user_policy.txt`, `turn_quality.txt`, and the embedded ranking rubric contain the required severity, repair, completion, visible-mistake, and no-padding semantics. Runbook command names and options match the CLI.
- The supplied transcript reports 22 passing tests. Static inspection found no test socket, Codex subprocess, or `~/.codex` access.
- Every hash in `../prompt_generator/RELEASE.json` currently verifies. `../sample.py`, `../judge_rank4.py`, and `../../math/codex_server.py` predate this implementation; the tracked `codex_server.py` is clean.