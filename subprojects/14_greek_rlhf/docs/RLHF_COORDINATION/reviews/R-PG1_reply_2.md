# Reply to R-PG1 review cycle 2

Spec and glossary are now v1.0.2, frozen 17 Sept 2026 (R-PG1 cycle-2 fixes applied without a third cycle). Line numbers refer to the v1.0.2 files.

1. Deferred, by assignment of ownership rather than a definition here. Spec decision 8 (`docs/SEED_LABEL_SPEC_20260917.md:304`) now says the deterministic transition/priority table (visible event and user state → one move, tie-breaks, terminal cases) is owned by the dialogue workstream (`DIALOGUE_V2_EXECUTION_PLAN_20260917.md` §4, Stage B1 adaptive user implementation) and frozen there at R-PG6, and that it does not block single-turn generation. Spec §4.7 (`:163`) and the glossary (`data/rlhf/prompts/seed_label_definitions_v1.md:92`) label the moves "adopted list; selection rule owned by the dialogue workstream"; the "proposed / mixture not decided" wording is gone.
2. Changed. A cell is primary purpose × language (42 cells = 6 × 7) in spec §5.4 (`:235`), and `task_type` is reserved for forum F1–F9 analysis (spec `:147`, glossary `:52`). The two-stream plan now uses the same unit at `RLHF_TWO_STREAM_EXECUTION_PLAN_20260917.md:13`, `:72`, `:97`, `:120-121`, `:154`, `:172`.
3. Changed. The deficit is T[c] minus "validated eligible active pair credit E[c]" (spec `:258`, ledger E at `:283`). "Held" now means quarantine only (spec `:240`).
4. Changed. The four turn fields are exactly `move`, `new_information`, `trigger`, `changes_task` (spec `:177`, glossary `:106`). The dialogue plan uses these literal names and lists the earlier draft names (`DIALOGUE_V2_EXECUTION_PLAN_20260917.md:76`). The dialogue v2 code already emits these four names.
5. Changed. Provenance is split into `generator_version` {0.1 (defunct, archival), 0.2-prototype, 0.2, 0.3}, `source_route` {forum-gate-v3, seeded, dialogue}, `dialogue_protocol_version` {none, dialogue-quality-depth-v1, dialogue-v2} and `opening_generator_version` (spec `:246-251`, import contract `RLHF_TWO_STREAM_EXECUTION_PLAN_20260917.md:89`). Combined values such as `dialogue-quality-depth-v1 (0.1 seeds)` map to protocol dialogue-quality-depth-v1 + opening generator 0.1 (spec `:251`). Recorded default for review: forum rows from rounds 1–2 record `generator_version=0.2-prototype`, and dialogue rows (pilot included) record `0.3`. The registry and inventory code still write combined values; those files were out of scope for this pass.
6. Changed. Spec §5.4 defines source lineage (`:241`: same forum post or seed; controls split assignment and duplicate-alias grouping) and task family (`:242`: shared task, scenario or mathematical structure across sources; controls coverage caps and near-duplicate screening). It also defines deduplication (`:243`: instance key plus similarity screening). The ingestion plan's table and lineage wording now match (`PROMPT_INGESTION_AND_NEXT_500_PLAN_20260917.md:37`, `:42`, `:56-57`, `:62`). The existing `source_family` code field carries the source lineage (spec `:241`).
7. Changed. Spec §5.1 has an exclusive order (calculation > translation > create > advice > opinion > explanation > question > share > other) with two boundary examples (`:217`); the glossary's Forum task_type section repeats it in one sentence (`:73`). `relabel_v1.jsonl` was produced under glossary v1.0.1, before this rule; overlapping rows are not relabelled in this documentation-only pass.
8. Changed. The spec title and status line both say frozen v1.0.2 (`:1`, `:3`), `anxious` is "defined, not drawn" in both files (spec `:104`, glossary `:12`), and the §4/§5 headings no longer say "Proposed". The two-stream plan cites spec v1.0.2 (`:32`, `:71`, `:250`). `CURRENT_VERSIONS.md:37` still says v1.0.1 and "synced with v0.3": that file is owned by the orchestrator and was not edited here.
9. Changed. `docs/RLHF_COORDINATION/reviews/R-PG1_term_manifest_v1.0.2.md` lists all 170 bold-marked spec terms with section anchor, line and glossary status (yes 108, spec-only 56, no 6), the 8 excluded emphasis occurrences, and the exact script that produced them. It exposes one open gap: the glossary uses "serious error" without its definition.

Also applied (owner clarification): the Detail definition now says that values that define the task are part of the ask, not context (spec `:156`, glossary `:61`).

No third review cycle (owner rule: do not block on reviews).
