VERDICT: HOLD

1. **Blocking — round-1 data remains in training.** [export.py:45](/Users/foivoskarounos-zamparloukos/Projects/train-apertus-with-glossapi/subprojects/12_greek_sft_experiments/data/rlhf/dpo01/export.py:45) checks the prompt’s `round_first`, not the selected judgement’s alias/file. Consequently record `fp:7ed1af66404bf003` appears at [provenance.jsonl:55](/Users/foivoskarounos-zamparloukos/Projects/train-apertus-with-glossapi/subprojects/12_greek_sft_experiments/data/rlhf/dpo01/provenance.jsonl:55) in `train`, using judgement `ac5c02599aaec336eb09`, alias `forum:mathematica:7ed1af6640`, and `round1_all_samples.jsonl`. This violates the owner’s complete round-1 exclusion and contaminates every arm. Fix: classify the selected judgement through `aliases.round` and `judgements.file`, exclude this row, and regenerate every frozen output and manifest count. Do not substitute the round-2 alias unless it has independently recorded eligible pair credit.

2. **Blocking — unrelated round-3 prompts were collapsed into four run-level groups, leaving zero round-3 rows in dev.** [export.py:72](/Users/foivoskarounos-zamparloukos/Projects/train-apertus-with-glossapi/subprojects/12_greek_sft_experiments/data/rlhf/dpo01/export.py:72) assigns `seed_id=r.get('run')`; [export.py:99](/Users/foivoskarounos-zamparloukos/Projects/train-apertus-with-glossapi/subprojects/12_greek_sft_experiments/data/rlhf/dpo01/export.py:99) then unions every row sharing that run. For example, `R3-0001` and `R3-0003` have different logical IDs, source IDs and prompt hashes but share group `pfx:387622…` at [provenance.jsonl:203](/Users/foivoskarounos-zamparloukos/Projects/train-apertus-with-glossapi/subprojects/12_greek_sft_experiments/data/rlhf/dpo01/provenance.jsonl:203) and [provenance.jsonl:204](/Users/foivoskarounos-zamparloukos/Projects/train-apertus-with-glossapi/subprojects/12_greek_sft_experiments/data/rlhf/dpo01/provenance.jsonl:204). The 214 round-3 rows form four groups of 169, 27, 11 and 7 rows, all assigned to train. `run` is not a seed-family relationship, so criterion 4’s recorded component/hash split was not applied correctly and the held-out set excludes the entire round-3 population. Fix: join genuine recorded seed/source-family links; otherwise fall back to logical/source ID or canonical prompt hash. Use an order-independent canonical component key, then recompute the hash split.

3. **Major — round-3 provenance does not uniquely identify the judgement that produced a pair.** [export.py:71](/Users/foivoskarounos-zamparloukos/Projects/train-apertus-with-glossapi/subprojects/12_greek_sft_experiments/data/rlhf/dpo01/export.py:71) stores a synthetic stage/batch ID while setting `rubric_sha=None`; it omits the judgement pass and source record. Twenty-four exported selections have multiple pass-1/pass-2 judgements under the same recorded ID. For example, `R3-0023` at [provenance.jsonl:211](/Users/foivoskarounos-zamparloukos/Projects/train-apertus-with-glossapi/subprojects/12_greek_sft_experiments/data/rlhf/dpo01/provenance.jsonl:211) says only `s1b0`, while the judgement file contains different pass-1 and pass-2 rankings under that key. Five exported pairs, including `R3-0067` at [provenance.jsonl:228](/Users/foivoskarounos-zamparloukos/Projects/train-apertus-with-glossapi/subprojects/12_greek_sft_experiments/data/rlhf/dpo01/provenance.jsonl:228), depend specifically on pass 2. This defeats the stage goal of unambiguous judgement traceability. Fix: record judgement file, pass, stage, batch, exact record hash/line, rubric hash and sample-file identity for every row; hash those judgement files in the manifest.

4. **Major — the manifest is not the required complete freeze receipt.** [manifest.json:78](/Users/foivoskarounos-zamparloukos/Projects/train-apertus-with-glossapi/subprojects/12_greek_sft_experiments/data/rlhf/dpo01/manifest.json:78) contains word summaries rather than exact tokenizer-bound counts, [manifest.json:84](/Users/foivoskarounos-zamparloukos/Projects/train-apertus-with-glossapi/subprojects/12_greek_sft_experiments/data/rlhf/dpo01/manifest.json:84) has only truncated hashes for a subset of inputs, and [manifest.json:97](/Users/foivoskarounos-zamparloukos/Projects/train-apertus-with-glossapi/subprojects/12_greek_sft_experiments/data/rlhf/dpo01/manifest.json:97) lists outputs without hashes. It also omits the required output-language distribution and hashes for pool prompts/samples and round-3 judgement files. Thus criterion 6 cannot bind the four arms to exact data and provenance. Fix: after correcting findings 1–3, record full SHA-256 hashes for every input/output, exact per-split tokenizer counts and maxima, output-language distribution, judgement and benchmark-cache identities, and recomputed exclusion/group distributions.

Verified correct:

- All 398 present rows’ prompt/chosen/rejected text matches its named source sample byte-for-byte; chosen/rejected indices match the recorded ranking once the exact judgement pass is reconstructed.
- The current export has 398 distinct logical IDs and 398 distinct normalized prefixes.
- Every prompt ends on a user turn; chosen and rejected are single assistant messages; output rows contain only `prompt`, `chosen`, and `rejected`. All selected samples finished with `stop`.
- The 24 recorded dedicated-maths pairs are excluded, 66 embedded-maths rows are retained, five superseded round-3 rows are excluded, and all eight development-dialogue pairs remain `training_eligible=false` and absent.
- A fresh scan against all 14 current benchmark caches found zero overlaps. Exact Apertus-template tokenization found zero examples above 4,096 tokens; the maximum was 1,803.
- The manifest’s stated row and distribution counters and its listed source-hash prefixes match the current files, notwithstanding the incomplete receipt and invalid selection/split above.