# R-RL2 reply, cycle 1 (17 September 2026)

No second cycle (owner rule: do not block on reviews). All five findings were acted on; the report is regenerated as maths-v1.2.

1. **Eligibility invariants not enforced — fixed.** `maths_judge.py` now computes `eligible_positive` deterministically from the judged fields: a usable reference, outcome correct, task complete against what the user asked, reasoning not invalid, and a valid explanation whenever one was requested; on an ambiguous reference also `handled_ambiguity = yes`. The verdict is synchronised (a reinforce that fails the rule becomes neutral), and the model's own values are kept as `verdict_model` and `eligible_positive_model`. Eight regression tests in `test_maths_judge.py` cover invalid derivations, short correct answers with no explanation requested, incomplete answers, ambiguity handling, solver disagreement and a model flag that alone tries to create a positive. On the rerun the deterministic rule and the judge's own flag disagreed 0 times, so the judge was already self-consistent; the rule now enforces it structurally rather than trusting it.
2. **Contradictory ambiguity contract — fixed.** The rubric's blanket "flagged items are held until resolved" and "reinforce requires a verified reference" are gone; the text now distinguishes ambiguous-and-agreed from unresolved and states the `handled_ambiguity` requirement. Eligibility for such items is computed, not merely removable.
3. **The zero-pair conclusion — re-tested, and it stands.** `g2:R2B11_009` candidate 1 was re-judged under v1.2: it computes the 60–240 g range correctly, but the question is ambiguous about which dose goes with which timing, and the reply does not name that ambiguity (`handled_ambiguity = no`), so it is not an eligible positive on an ambiguous reference. Across all 41 pre-existing items, 21 replies did name the ambiguity and none of them was both correct and complete. The report now states forum and seeded yields together with the per-item evidence, and says plainly that this is a measurement of the model, not of judge strictness.
4. **v1.1 versus v1.2 comparison — addressed.** The report compares the same candidate set under one rubric version and records the deterministic-rule disagreement count (0), which is the part that isolates the rule from rerun variance. Per-candidate judge variance between reruns remains and is not presented as a rule effect.
5. **v1 backup not reproducible — fixed.** The original v1 rubric text was reconstructed and verified against the hash recorded in the v1 judgements (`6e438e50ca065dd6`), archived as `rubric_maths_v1.0_archived.txt`, and the backup runner now reads that file instead of the mutable current rubric.
