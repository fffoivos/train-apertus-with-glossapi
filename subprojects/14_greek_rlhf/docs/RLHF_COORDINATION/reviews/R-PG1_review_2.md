VERDICT: HOLD

1. **Blocking — decision 8 still lacks an operational default.** `docs/SEED_LABEL_SPEC_20260917.md:295` calls move selection deterministic by reference to the dialogue plan, but `docs/RLHF_COORDINATION/DIALOGUE_V2_EXECUTION_PLAN_20260917.md:67-74` supplies an unordered set of permissible reactions. Several moves can apply to the same state, with no precedence or tie-break. The glossary still calls the moves proposed and the mixture undecided at `data/rlhf/prompts/seed_label_definitions_v1.md:89-102`. Implementations can therefore diverge before the diagnostic. Define a total transition rule or priority table from visible event and user state to one move, including tie-breaking and terminal cases; synchronize its adopted status in the glossary.

2. **Blocking — “cell” still alternates between primary purpose and task type.** The spec distinguishes primary purpose, task family and task type at `docs/SEED_LABEL_SPEC_20260917.md:126-146`, then defines a cell as task type × language at `:233`. The master plan repeats task type × language at `docs/RLHF_COORDINATION/RLHF_TWO_STREAM_EXECUTION_PLAN_20260917.md:12-15` and `:31-39`, while `data/rlhf/target_distribution_v1.json:6-8` and `docs/RLHF_COORDINATION/PROMPT_INGESTION_AND_NEXT_500_PLAN_20260917.md:99-108` define the 42 cells as primary purpose × language. This changes the allocation unit and can produce incompatible quota tables. Define `cell` exclusively as primary purpose × language throughout; reserve `task_type` for F1–F9 analysis.

3. **Blocking — the deficit definition counts formally excluded “held” pairs.** `docs/SEED_LABEL_SPEC_20260917.md:238` defines held/quarantined material as excluded from the active view, but `:249` subtracts “accepted pairs already held” when computing a deficit. This contradicts the E ledger at `:274` and the ingestion formula at `docs/RLHF_COORDINATION/PROMPT_INGESTION_AND_NEXT_500_PLAN_20260917.md:101-110`, where holds are excluded. It can directly understate generation deficits. Replace “accepted pairs already held” with “validated eligible active pair credit E[c]”; use “held” only for quarantine.

4. **Major — dialogue turn metadata have two incompatible field vocabularies.** The spec requires `move`, `new_information`, `trigger`, and `changes_task` at `docs/SEED_LABEL_SPEC_20260917.md:175-176`. The dialogue plan instead says to save `strategy`, `new_information`, `what_visible_event_triggered_it`, and “whether it changes the task” while claiming to use the spec’s field names at `docs/RLHF_COORDINATION/DIALOGUE_V2_EXECUTION_PLAN_20260917.md:76`. This will produce incompatible schemas. Choose one exact four-field schema and use those literal names in both documents.

5. **Major — the earlier `generator version` finding remains unresolved.** The spec’s closed list mixes generator versions, a forum gate/source route and a dialogue protocol at `docs/SEED_LABEL_SPEC_20260917.md:242`; the import contract says the field is only 0.2 or 0.3 at `docs/RLHF_COORDINATION/RLHF_TWO_STREAM_EXECUTION_PLAN_20260917.md:88-89`. Actual code emits another combined value, `dialogue-quality-depth-v1 (0.1 seeds)`, at `data/rlhf/inventory.py:43`, while `data/rlhf/registry/registry.py:55-57` records a similar out-of-list seed value. Provenance therefore remains non-validatable under one schema. Define separate fields for generator version, source/gate version, dialogue protocol version and opening generator version, with explicit archival enums.

6. **Major — `source family` has conflicting identity boundaries.** `docs/SEED_LABEL_SPEC_20260917.md:239` limits a source family to prompts derived from the same source. `docs/RLHF_COORDINATION/PROMPT_INGESTION_AND_NEXT_500_PLAN_20260917.md:54-59` also groups prompts by shared mathematical structure, scenario or underlying task. Those rules can assign unrelated sources to the same split or count them differently for coverage. Separate source lineage from semantic/task family, then state which key controls deduplication, coverage caps and split assignment.

7. **Major — F1–F9 still cannot be assigned exclusively.** At `docs/SEED_LABEL_SPEC_20260917.md:205-215`, a determinate numeric question satisfies both F1 `question` and F6 `calculation`; a request to translate supplied text satisfies both F5 `translation` and F9 `create`. The boundary paragraph does not resolve these overlaps, although each forum row must receive exactly one task type. Add an exclusive precedence order and boundary examples, mirrored in `data/rlhf/prompts/seed_label_definitions_v1.md:63-73`.

8. **Minor — the frozen status and `anxious` status are inconsistent.** The spec is titled frozen v1.0.1 at `docs/SEED_LABEL_SPEC_20260917.md:1` but says draft/not reviewed at `:3`; it calls `anxious` proposed at `:103`, while decision 1 adopts it at `:288` and the glossary calls it defined at `data/rlhf/prompts/seed_label_definitions_v1.md:12`. The master plan still calls the shared spec draft v0.3 at `docs/RLHF_COORDINATION/RLHF_TWO_STREAM_EXECUTION_PLAN_20260917.md:71`, and `docs/RLHF_COORDINATION/CURRENT_VERSIONS.md:37` says v1.0.1 but also says the glossary is synchronized with v0.3. Choose one candidate version/status and update all four locations together.

9. **Minor — the term-coverage evidence is not reviewable.** `docs/RLHF_COORDINATION/reviews/R-PG1_term_coverage.txt:1` contains only “69 terms; none missing,” without the 69 terms, their source locations, their spec anchors or the command that produced the result. It did not expose the cell, source-family or metadata conflicts above. Publish the term-by-term manifest and its reproducible check.

**Verified as correct**

- Purpose, language, difficulty, attitude and register weights match `design.json` and `target_distribution_v1.json`; each margin sums to 100%.
- T5’s mathematics definition, reference semantics and explanation rule agree with the specialist judge plan §A3.
- The serious-error threshold now matches `turn_quality.txt:3`; prevention, supported recovery and healthy continuation boundaries agree with dialogue plan §6.
- The calculation correction is present in the new registry: alias `forum:mathematica:0607` resolves to `primary_purpose=math`, `task_type=calculation`, `maths_content=maths`.
- Generic difficulty is authoritative for generator 0.2+, and the glossary carries the same definitions.
- Bare/terse ceilings now agree between spec and glossary.
- Safety sub-kinds now have an exclusive decision order.
- Recorded SHA-256 prefixes for the reviewed spec, glossary, plans, distribution and code match the current files.

